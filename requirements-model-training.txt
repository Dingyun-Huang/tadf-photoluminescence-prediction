# HGNN (heterogeneous GraphSAGE) training dependencies.
#
# Install PyTorch and PyG extension wheels BEFORE this file — see README.md.
#
# Step 1 — PyTorch (pick ONE line for your platform):
#   pip install torch==2.4.1 --index-url https://download.pytorch.org/whl/cu124
#   pip install torch==2.4.1 --index-url https://download.pytorch.org/whl/cpu
#
# Step 2 — PyG extensions (match your PyTorch/CUDA build):
#   pip install pyg-lib torch-scatter torch-sparse torch-cluster torch-spline-conv -f https://data.pyg.org/whl/torch-2.4.0+cu124.html
#   pip install pyg-lib torch-scatter torch-sparse torch-cluster torch-spline-conv -f https://data.pyg.org/whl/torch-2.4.0+cpu.html
#
# Step 3 — remaining packages:
#   pip install -r requirements-model-training.txt

torch-geometric==2.6.1
rdkit==2025.9.1
wandb==0.22.3
PyYAML==6.0.2
matplotlib==3.10.1
scikit-learn==1.8.0
numpy==2.1.3
tqdm==4.67.1
torchinfo==1.8.0
